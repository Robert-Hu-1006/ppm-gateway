apiVersion: 1

providers:
  - name: Default # A uniquely identifiable name for the provider
    orgId: 1
#   folder: ''
#   folderUid: ''
    type: file
    #allowUiUpdates: true
    options:
      # Docker Path
      path: /etc/grafana/provisioning/dashboards
      